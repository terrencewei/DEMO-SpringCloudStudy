dependencies {
    compile libs.springcloud
    compile libs.data
    compile libs.log
    compile group: 'com.alibaba', name: 'fastjson', version: '1.2.51'
}

jar {
    enabled = true
}

bootJar {
    enabled = false
}